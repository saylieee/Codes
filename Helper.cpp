28/9/2019

int min = *min_element(v.begin(),v.end());

/*
 min is  assigned with minimum value of vector.
 Use:- no need to run loop to find min value.
 similary *max_element(v.begin(),v.end()) for maximum element.
 */
----------------------------------------------------------------------

reverse(v.begin(),v.end());
/*
 reverses the contents of vector.
 */

----------------------------------------------------------------------

vector<int> a;
a.resize(n);

/*
 If you want to new vector of similar size
 */

----------------------------------------------------------------------

fill(v.begin(), v.end(), 0);
/*
 set all elements in vector to 0.
 */
-----------------------------------------------------------------------

